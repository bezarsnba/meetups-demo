apiVersion: apps/v1
kind: Deployment
metadata:
  name: podinfo
  namespace: demo-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: podinfo
  template:
    metadata:
      labels:
        app: podinfo
    spec:
      containers:
        - name: podinfo
          image: stefanprodan/podinfo:6.6.2
          ports:
            - containerPort: 9898
